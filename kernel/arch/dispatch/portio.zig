// Port-I/O virtualization dispatch surface. Currently a placeholder —
// the spec-v3 port_io fault decoder lives in `arch/x64/portio.zig` but
// is not wired into the kernel until the relevant syscall path lands.
