/// VM exit dispatch — placeholder. The spec-v3 vm_exit delivery flows
/// through the recv/reply lifecycle on the vCPU EC's `exit_port`; this
/// file is retained as the future home of the per-arch exit classifier
/// once that path is wired up.
